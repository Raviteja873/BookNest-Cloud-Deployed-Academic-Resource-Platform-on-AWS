# Package Notes

This package contains GitHub-ready documentation and the reviewed architecture diagram.

Copy the actual application source (`app.py`, `templates/`, `static/`, and `LICENSE`) from the existing BookNest project into the repository root. Do **not** copy `database.db` into Git.

The documentation intentionally uses placeholders for GitHub identity, AWS host/IP values, ALB DNS names, SSH key paths, and secrets.
